// HUD overlay — three text labels updated each frame by a modded Hud class.
// Load via: GetGame().GetWorkspace().CreateWidgets("gui/layouts/MyMod/mymod_hud_overlay.layout", hudRoot)
//
// Script companion (Scripts/5_Mission/MyMod_Hud.c):
//   modded class Hud {
//     protected TextWidget m_lblStatus;
//     protected TextWidget m_lblSub;
//     protected TextWidget m_lblWarn;
//     override void Init(Widget hud_panel_w) {
//       super.Init(hud_panel_w);
//       Widget overlay = GetGame().GetWorkspace().CreateWidgets("gui/layouts/MyMod/mymod_hud_overlay.layout", hud_panel_w);
//       m_lblStatus = TextWidget.Cast(overlay.FindAnyWidget("LabelMain"));
//       m_lblSub    = TextWidget.Cast(overlay.FindAnyWidget("LabelSub"));
//       m_lblWarn   = TextWidget.Cast(overlay.FindAnyWidget("LabelWarning"));
//     }
//     override void Update(float timeslice) {
//       super.Update(timeslice);
//       PlayerBase player = PlayerBase.Cast(GetGame().GetPlayer());
//       if (!player) return;
//       m_lblStatus.SetText(string.Format("HP: %1", Math.Round(player.GetHealth("", "Health"))));
//       m_lblSub.SetText(string.Format("Blood: %1", Math.Round(player.GetHealth("", "Blood"))));
//       m_lblWarn.Show(player.GetHealth("", "Health") < 20.0);
//     }
//   }

FrameWidgetClass HudOverlay {
 ignorepointer 1
 position 0 0
 size 1 1
 hexactpos 0
 vexactpos 0
 hexactsize 0
 vexactsize 0
 {
  TextWidgetClass LabelMain {
   ignorepointer 1
   color 1 1 1 0.9
   position 16 16
   size 200 24
   halign right_ref
   hexactpos 0
   vexactpos 1
   hexactsize 1
   vexactsize 1
   text "Status: OK"
   font "gui/fonts/sdf_MetronBook24"
   "exact text" 1
   "exact text size" 14
   "text halign" right
   "text valign" center
  }
  TextWidgetClass LabelSub {
   ignorepointer 1
   color 0.8627 0.8627 0.8627 0.8
   position 16 44
   size 200 20
   halign right_ref
   hexactpos 0
   vexactpos 1
   hexactsize 1
   vexactsize 1
   text ""
   font "gui/fonts/sdf_MetronLight24"
   "exact text" 1
   "exact text size" 12
   "text halign" right
   "text valign" center
  }
  TextWidgetClass LabelWarning {
   visible 0
   ignorepointer 1
   color 1 0.2 0.2 1
   position 0 16
   size 400 28
   halign center_ref
   valign bottom_ref
   hexactpos 0
   vexactpos 0
   hexactsize 1
   vexactsize 1
   text "WARNING: LOW HEALTH"
   font "gui/fonts/sdf_MetronBook24"
   "exact text" 1
   "exact text size" 16
   "bold text" 1
   "text halign" center
   "text valign" center
  }
 }
}