# Agentic-Z

**An AI Agent Stack for DayZ Modding**, with first-class support for Claude Code, Codex, and Gemini. Install it as a plugin (or clone the repo), point it at your mod project, and your AI shows up DayZ-fluent on day one — opinionated rules, specialist agents, slash-command skills, and a local RAG over the vanilla engine source.

## Quick start

Install the plugin from any Claude Code session, then run the wizard:

<QuickStartTerminal align="left" />

Two install paths (plugin or clone), full prerequisites, and troubleshooting on the **[Install & Setup](./install)** page.

## DayZ Modding workflow

`/dayz-init` is the front door — one slash command takes you from empty repo to mod-loaded-in-DayZ. First run is a setup wizard (env check, intent prompts, plan, execute). Every run after that drops you into a mission-control hub for the cached project: build & launch, stop diag, tail server log, open Workbench / Object Builder, switch project, etc.

The wizard wraps the lower-level skills below. Power users can still call them directly when they want surgical control.

| Command | Purpose |
|---|---|
| [`/dayz-init`](./skills/dayz-init) | **Front door.** Setup wizard on first run, mission-control hub thereafter |
| [`/dayz-preflight`](./skills/dayz-preflight) | Verify environment (P:\ mounted, Tools installed, vanilla data extracted) |
| [`/dayz-workdrive`](./skills/dayz-workdrive) | Mount `P:\` without opening DayZ Tools (auto-resolves work drive from `settings.ini`) |
| [`/dayz-new-mod`](./skills/dayz-new-mod) | Scaffold `workspace/&lt;ModName&gt;/` + create `P:\&lt;ModName&gt;\` junction |
| [`/dayz-import-mod`](./skills/dayz-import-mod) | Symlink an existing mod folder into the workspace + create the junction |
| [`/dayz-build-pbo`](./skills/dayz-build-pbo) | Pack and deploy to `P:\Mods\@&lt;ModName&gt;\Addons\&lt;ModName&gt;.pbo` |
| [`/dayz-add-server`](./skills/dayz-add-server) | Set up a test server instance under `.server/&lt;instance&gt;/` |
| [`/dayz-launch-test`](./skills/dayz-launch-test) | Start local Diag server + client with the mod loaded |
| [`/dayz-stop-test`](./skills/dayz-stop-test) | Kill running DayZDiag_x64.exe processes |
| [`/dayz-cot-bootstrap`](./skills/dayz-cot-bootstrap) | One-shot Community-Online-Tools admin setup |
| [`/dayz-pack-texture`](./skills/dayz-pack-texture) | PNG/TGA → `.paa` via ImageToPAA |
| [`/dayz-build-imageset`](./skills/dayz-build-imageset) | Pack `.gui-sources/&lt;setname&gt;/*.png` into a DayZ `.imageset` + `.paa` atlas with a 10px gutter |
| [`/dayz-edit-types`](./skills/dayz-edit-types) | Programmatically edit a single `&lt;type&gt;` in `types.xml` |
| [`/dayz-split-types`](./skills/dayz-split-types) | Split monolithic `types.xml` into 18 categorized files |
| [`/dayz-search-download`](./skills/dayz-search-download) | Pull prebuilt vector index from GitHub releases (recommended fast path) |
| [`/dayz-search-index`](./skills/dayz-search-index) | Build the semantic-search index over vanilla DayZ source yourself |
| [`/dayz-search-wiki-index`](./skills/dayz-search-wiki-index) | Index the Bohemia community wiki into the same DB |
| [`/dayz-clean-workspace`](./skills/dayz-clean-workspace) | Remove DayZ scaffolds and their deployed artifacts |

**Specialist tools** (deep work): [`/dayz-p3d-audit`](./skills/dayz-p3d-audit), [`/dayz-p3d-debin`](./skills/dayz-p3d-debin), [`/dayz-particles`](./skills/dayz-particles).

**MCP server:** `dayz-rag` — exposes `search_dayz_source`, `search_dayz_wiki`, `get_dayz_file`, `list_indexed_sources` to every DayZ specialist agent. Backed by the index built via `/dayz-search-index` + `/dayz-search-wiki-index`.

**Full walkthrough** with prerequisites, env vars, and troubleshooting: [`dayz-modding`](./dayz-modding).

## DayZ specialist agents

Specialist agents live under `.claude/agents/` and cover the major DayZ surfaces:

| Agent | Focus |
|---|---|
| `dayz-asset-specialist` | `.p3d` / `.paa` / `.rvmat` and Workbench asset integration |
| `dayz-config-specialist` | `config.cpp`, CfgPatches, CfgVehicles, CfgWeapons, hidden selections |
| `dayz-map-specialist` | Terrain Builder, DayZ Editor, map objects, clutter, surfaces |
| `dayz-mod-debugger` | Log/RPT/crash analysis, BattlEye diagnosis, performance profiling |
| `dayz-mod-reviewer` | Audit `workspace/&lt;ModName&gt;/` for convention compliance; routes findings to the right specialist |
| `dayz-object-builder` | `.p3d` LODs, named selections, geometry, damage zones |
| `dayz-script-specialist` | Enforce Script — modded classes, RPCs, replication, gameplay logic |
| `dayz-server-admin` | `types.xml`, `init.c`, `cfggameplay.json`, server performance |
| `dayz-ui-specialist` | `.layout` files, widget scripting, HUD/menu, UI theme/color |
| `dayz-workbench-specialist` | Workbench plugin development (editor-time tooling) |

Plus `agent-creator` for scaffolding new agent definitions to the standard template.

## Where to next

- **[Install & Setup](./install)** — plugin or clone, full prerequisites, troubleshooting
- **[End-to-End Workflow](./dayz-modding)** — what `/dayz-init` orchestrates, in depth
- **[Rules & Conventions](./dayz-conventions)** — L2 DayZ rules every agent obeys
- **[Architecture](./architecture)** — how the L1 / L2 / L3 layers fit together (for power users and contributors)
- **[Contributing](./contributing)** — how to send fixes back, propose new agents, or improve existing skills
